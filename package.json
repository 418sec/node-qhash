{
  "name": "qhash",
  "version": "1.1.2",
  "main": "qhash.js",
  "description": "merge and deep-copy objects, get/set nested properties by dotted name",
  "author": "andras@andrasq.com",
  "license": "Apache-2.0",
  "repository": {
    "url": "git://github.com/andrasq/node-qhash",
    "type": "git"
  },
  "engines": {
    "node": ">=0.10"
  },
  "keywords": [ "merge", "extend", "assign", "get", "set", "deep-copy", "clone" ],
  "dependencies": {
  },
  "devDependencies": {
    "qnit": "0.16.0",
    "qassert": "1.2.0"
  },
  "scripts": {
    "test": "qnit test-*",
    "coverage": "nyc --reporter text --reporter lcov npm test",
    "clean": "rm -rf .nyc_output coverage out"
  }
}
