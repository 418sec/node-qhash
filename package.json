{
  "name": "qhash",
  "version": "1.3.0",
  "main": "qhash.js",
  "description": "merge and deep-copy objects, get/set properties by dotted name, taint detect",
  "author": "andras@andrasq.com",
  "license": "Apache-2.0",
  "repository": "git://github.com/andrasq/node-qhash",
  "engines": {
    "node": ">=0.10"
  },
  "keywords": [ "merge", "extend", "assign", "get", "set", "deep-copy", "backup", "compare", "diff", "taint" ],

  "dependencies": {
  },
  "devDependencies": {
    "qnit": "0.25.0",
    "qassert": "1.6.1"
  },

  "scripts": {
    "test": "qnit test-q*",
    "coverage": "nyc --reporter text --reporter lcov npm test",
    "clean": "rm -rf .nyc_output coverage out"
  }
}
